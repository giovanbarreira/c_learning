#include <stdio.h>  			// library
int main() {				// main is the entry point function
    printf("Hello, World!\n");		// write in console
    return 0;				// if return 0, than everything is ok
}					// end of the main function
